# Pinned to the versions the live stack actually runs (2026-07-13).
# A live-money bot must not absorb surprise upgrades: bump deliberately,
# run the test suite, then update the pin.
ccxt==4.5.56
pandas==3.0.3
numpy==2.3.5
ta==0.11.0
APScheduler==3.11.2
requests==2.32.5
python-dotenv==1.2.2
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
websocket-client==1.9.0    # liquidations.py WebSocket collector
pytest==9.1.0              # run_all.sh pre-flight + CI
