# ─────────────────────────────────────────────────────────────────────────────
# SECRETS / CREDENTIALS — NEVER COMMIT. Real Binance keys + Telegram token live
# in app/.env. The .env.example template (placeholders only) IS committed.
# ─────────────────────────────────────────────────────────────────────────────
.env
*.env
!.env.example
!app/.env.example

# ── Flask instance: user logins (users.db) + signal DBs + session secrets ──
instance/
*.db
*.db.*
*.sqlite
*.sqlite3

# ── Runtime logs ──
logs/
*.log
*.out

# ── Runtime state / locks ──
*.lock
bot.lock
circuit_state.json
bot_strategy.json
paper_active.json
paper_state_*.json
scan_results.json
strategy2_signals.json
strategy3_state.json
event_radar_state.json
tech_news_state.json
daily_report_state.json
morning_brief_state.json
price_alerts.json
tg_commands_state.json
tw_stocks_state.json
tw_intraday_state.json
line_ids.json
site_link_state.json
line_quota_state.json
tg_sent_log.json
tg_autoclean_state.json
watchdog_state.json
s3_halt.json
signal_outcomes.json
eth_mom_state.json
balance_history.json
whale_state.json
whale_addresses.json
s1_bybit_positions.json
copy_followers.json
copy_status.json
app/backups/

# ── Large generated caches / data dumps ──
ohlcv_cache.json
backtest_runs.json

# ── Old local archive (may contain stale creds / server config) ──
_archive/

# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/
ENV/

# ── Claude Code local settings (machine-specific) ──
.claude/settings.local.json

# ── OS / editor cruft ──
.DS_Store
.DS_Store?
._*
*.swp
*~
.idea/
.vscode/
.stack_stopped
app/research_cache/
